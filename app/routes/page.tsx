import PlaceholderPage from '@/components/PlaceholderPage';

export default function RoutesPage() {
    return <PlaceholderPage title="Routes" description="Optimize and assign geographic routes to field inspectors." />;
}
